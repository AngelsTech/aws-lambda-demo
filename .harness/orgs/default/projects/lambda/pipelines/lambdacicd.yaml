pipeline:
  name: lambda-cicd
  identifier: lambdacicd
  projectIdentifier: lambda
  orgIdentifier: default
  properties:
    ci:
      codebase:
        connectorRef: <+input>
        repoName: aws-lambda-demo
        build: <+input>
  variables:
    - name: environment
      type: String
      description: Deployment environment (dev/stage/prod)
      value: dev
  stages:
    - stage:
        name: Build_and_Package
        identifier: Build_and_Package
        type: CI
        spec:
          cloneCodebase: true
          infrastructure:
            type: KubernetesDirect
            spec:
              connectorRef: eksconnector
              namespace: harness-delegate-ng
              automountServiceAccountToken: true
              os: Linux
          execution:
            steps:
              - step:
                  name: Install_and_Package
                  identifier: Install_and_Package
                  type: Run
                  spec:
                    connectorRef: dockerhub
                    image: python:3.13
                    shell: Sh
                    command: |
                      echo "Installing dependencies and packaging Lambda..."
                      apt-get update -qq && apt-get install -y -qq zip unzip > /dev/null 2>&1

                      mkdir -p python
                      pip install -r test/devtest/requirements.txt -t python
                      zip -q -r layer_package.zip python
                      echo "✓ Lambda layer packaged."

                      cd test
                      zip -q -r ../lambda_package.zip devtest \
                        -x "**/.git/*" "**/README.md" "**/Jenkinsfile" "**/__pycache__/*" "**/*.pyc"
                      cd ..
                      echo "✓ Lambda function packaged."

                      ls -lh *.zip
                    outputVariables:
                      - name: LAYER_PACKAGE
                        value: layer_package.zip
                      - name: LAMBDA_PACKAGE
                        value: lambda_package.zip
    - stage:
        name: Deploy_Lambda
        identifier: Deploy_Lambda
        type: CI
        spec:
          cloneCodebase: true
          infrastructure:
            type: KubernetesDirect
            spec:
              connectorRef: eksconnector
              namespace: harness-delegate-ng
              automountServiceAccountToken: true
              os: Linux
          execution:
            steps:
              - step:
                  name: Publish_Lambda_Layer
                  identifier: Publish_Lambda_Layer
                  type: Run
                  spec:
                    connectorRef: dockerhub
                    image: amazon/aws-cli:2.15.0
                    shell: Sh
                    envVariables:
                      AWS_REGION: us-east-1
                      HARNESS_AWS_CONNECTOR_REF: awsconnector
                    command: |
                      echo "Publishing Lambda Layer..."
                      LAYER_NAME="hey-world-deps-<+pipeline.variables.environment>"

                      aws lambda publish-layer-version \
                        --layer-name $LAYER_NAME \
                        --zip-file fileb://layer_package.zip \
                        --compatible-runtimes python3.13 \
                        --description "Dependencies for hey-world Lambda (<+pipeline.variables.environment>)" \
                        --query Version --output text > layer_version.txt

                      LAYER_VERSION=$(cat layer_version.txt)
                      echo "LAYER_VERSION=$LAYER_VERSION" >> layer.env
                      echo "✓ Published layer version $LAYER_VERSION"
                    outputVariables:
                      - name: LAYER_VERSION
                        value: LAYER_VERSION
              - step:
                  name: Deploy_Lambda_Function
                  identifier: Deploy_Lambda_Function
                  type: Run
                  spec:
                    connectorRef: dockerhub
                    image: amazon/aws-cli:2.15.0
                    shell: Sh
                    envVariables:
                      AWS_REGION: us-east-1
                      HARNESS_AWS_CONNECTOR_REF: awsconnector
                    command: |
                      ENV_NAME="<+pipeline.variables.environment>"
                      FUNCTION_NAME="hey-world-demo-$ENV_NAME"
                      ZIP_FILE="lambda_package.zip"
                      LAYER_VERSION="<+pipeline.stages.Deploy_Lambda.spec.execution.steps.Publish_Lambda_Layer.output.outputVariables.LAYER_VERSION>"

                      ROLE_ARN=""
                      if [ "$ENV_NAME" = "dev" ]; then
                        ROLE_ARN="arn:aws:iam::529088259986:role/lambda_exec_role_dev"
                      elif [ "$ENV_NAME" = "stage" ]; then
                        ROLE_ARN="arn:aws:iam::529088259986:role/lambda_exec_role_stage"
                      elif [ "$ENV_NAME" = "prod" ]; then
                        ROLE_ARN="arn:aws:iam::529088259986:role/lambda_exec_role_prod"
                      else
                        echo "Unknown environment: $ENV_NAME"
                        exit 1
                      fi

                      echo "Deploying Lambda Function: $FUNCTION_NAME"

                      if ! aws lambda get-function --function-name $FUNCTION_NAME > /dev/null 2>&1; then
                        aws lambda create-function \
                          --function-name $FUNCTION_NAME \
                          --runtime python3.13 \
                          --role $ROLE_ARN \
                          --handler devtest.lambda_function.lambda_handler \
                          --zip-file fileb://$ZIP_FILE \
                          --layers arn:aws:lambda:us-east-1:529088259986:layer:hey-world-deps-$ENV_NAME:$LAYER_VERSION \
                          --timeout 30 --memory-size 256
                        aws lambda wait function-active --function-name $FUNCTION_NAME
                        echo "✓ Function created."
                      else
                        aws lambda update-function-code \
                          --function-name $FUNCTION_NAME \
                          --zip-file fileb://$ZIP_FILE
                        aws lambda wait function-updated --function-name $FUNCTION_NAME
                        aws lambda update-function-configuration \
                          --function-name $FUNCTION_NAME \
                          --layers arn:aws:lambda:us-east-1:529088259986:layer:hey-world-deps-$ENV_NAME:$LAYER_VERSION \
                          --timeout 30 --memory-size 256
                        echo "✓ Function updated."
                      fi
              - step:
                  name: Setup_CloudWatch_Schedule
                  identifier: Setup_CloudWatch_Schedule
                  type: Run
                  spec:
                    connectorRef: dockerhub
                    image: amazon/aws-cli:2.15.0
                    shell: Sh
                    envVariables:
                      AWS_REGION: us-east-1
                      HARNESS_AWS_CONNECTOR_REF: awsconnector
                    command: |
                      ENV_NAME="<+pipeline.variables.environment>"
                      FUNCTION_NAME="hey-world-demo-$ENV_NAME"
                      RULE_NAME="hello-world-schedule-$ENV_NAME"
                      SCHEDULE="rate(5 minutes)"
                      STATEMENT_ID="cw-invoke-$ENV_NAME"

                      echo "Setting up CloudWatch Event Rule..."
                      aws events put-rule \
                        --name $RULE_NAME \
                        --schedule-expression "$SCHEDULE" \
                        --state ENABLED \
                        --description "Trigger hey-world Lambda every 5 minutes - $ENV_NAME"

                      FUNCTION_ARN=$(aws lambda get-function \
                        --function-name $FUNCTION_NAME \
                        --query 'Configuration.FunctionArn' \
                        --output text)

                      aws events put-targets \
                        --rule $RULE_NAME \
                        --targets "Id"="1","Arn"="$FUNCTION_ARN"

                      SOURCE_ARN=$(aws events describe-rule \
                        --name $RULE_NAME \
                        --query 'Arn' --output text)

                      if ! aws lambda get-policy --function-name $FUNCTION_NAME 2>/dev/null | grep -q $STATEMENT_ID; then
                        aws lambda add-permission \
                          --function-name $FUNCTION_NAME \
                          --statement-id $STATEMENT_ID \
                          --action 'lambda:InvokeFunction' \
                          --principal events.amazonaws.com \
                          --source-arn $SOURCE_ARN
                        echo "✓ Permission added."
                      else
                        echo "✓ Permission already exists, skipping."
                      fi
              - step:
                  name: Test_Lambda_Function
                  identifier: Test_Lambda_Function
                  type: Run
                  spec:
                    connectorRef: dockerhub
                    image: amazon/aws-cli:2.15.0
                    shell: Sh
                    envVariables:
                      AWS_REGION: us-east-1
                      HARNESS_AWS_CONNECTOR_REF: awsconnector
                    command: |
                      ENV_NAME="<+pipeline.variables.environment>"
                      FUNCTION_NAME="hey-world-demo-$ENV_NAME"

                      echo "Testing Lambda function..."
                      aws lambda invoke \
                        --function-name $FUNCTION_NAME \
                        --payload '{}' \
                        --cli-binary-format raw-in-base64-out \
                        lambda-response.json

                      echo "Lambda output:"
                      cat lambda-response.json
