pipeline:
  name: lambda-cicd
  identifier: lambdacicd
  projectIdentifier: lambda
  orgIdentifier: default
  tags: {}
  properties:
    ci:
      codebase:
        connectorRef: <+input>
        repoName: aws-lambda-demo
        build: <+input>
  stages:
    - stage:
        name: Build_and_Package
        identifier: Build_and_Package
        type: CI
        spec:
          cloneCodebase: true
          infrastructure:
            type: KubernetesDirect
            spec:
              connectorRef: eksconnector
              namespace: harness-delegate-ng
              automountServiceAccountToken: true
              nodeSelector: {}
              os: Linux
          caching:
            enabled: true
            key: lambda-build-<+pipeline.executionId>
            paths:
              - layer_package.zip
              - lambda_package.zip
          execution:
            steps:
              - step:
                  name: Select_Environment
                  identifier: Select_Environment
                  type: Run
                  spec:
                    connectorRef: eksconnector
                    image: alpine:latest
                    shell: Sh
                    command: |
                      echo "Select deployment environment:"
                      echo "1) dev"
                      echo "2) stage"
                      echo "3) prod"
                      ENV_NAME="<+input>.default(dev)"
                      echo "Deploying to environment: $ENV_NAME"
                      echo "ENV_NAME=$ENV_NAME" >> env.properties
                    outputVariables:
                      - name: ENV_NAME
                        type: String
                        value: ENV_NAME
              - step:
                  name: Install_Dependencies
                  identifier: Install_Dependencies
                  type: Run
                  spec:
                    connectorRef: eksconnector
                    image: python:3.13
                    shell: Sh
                    command: |
                      echo "Installing system dependencies..."
                      apt-get update && apt-get install -y zip
                      echo "Installing Python dependencies..."
                      mkdir -p python
                      pip install -r test/devtest/requirements.txt -t python
                      zip -r layer_package.zip python
                      echo "Layer package created successfully"
                      ls -lh layer_package.zip
              - step:
                  name: Package_Lambda
                  identifier: Package_Lambda
                  type: Run
                  spec:
                    connectorRef: eksconnector
                    image: python:3.13
                    shell: Sh
                    command: |
                      echo "Installing zip utility..."
                      apt-get update && apt-get install -y zip
                      echo "Zipping Lambda function code..."
                      cd test
                      zip -r ../lambda_package.zip devtest \
                        -x "**/.git/*" \
                        -x "**/README.md" \
                        -x "**/Jenkinsfile" \
                        -x "**/__pycache__/*" \
                        -x "**/*.pyc"
                      cd ..
                      echo "Lambda package contents:"
                      unzip -l lambda_package.zip
                      echo "Package size:"
                      ls -lh lambda_package.zip
    - stage:
        name: Deploy_Lambda
        identifier: Deploy_Lambda
        type: CI
        spec:
          cloneCodebase: true
          infrastructure:
            type: KubernetesDirect
            spec:
              connectorRef: eksconnector
              namespace: harness-delegate-ng
              automountServiceAccountToken: true
              nodeSelector: {}
              os: Linux
          caching:
            enabled: false
          execution:
            steps:
              - step:
                  name: Publish_Lambda_Layer
                  identifier: Publish_Lambda_Layer
                  type: Run
                  spec:
                    connectorRef: eksconnector
                    image: python:3.13
                    shell: Sh
                    command: |
                      echo "Using Harness AWS connector: awsconnector"
                      ENV_NAME="<+pipeline.variables.environment>"
                      if [ -z "$ENV_NAME" ] || [ "$ENV_NAME" = "null" ]; then
                        ENV_NAME="dev"
                      fi
                      LAYER_NAME="hey-world-deps-$ENV_NAME"
                      echo "Publishing Lambda Layer: $LAYER_NAME for environment: $ENV_NAME"
                      harness_aws_connector="awsconnector"
                      export AWS_DEFAULT_REGION=us-east-1
                      aws lambda publish-layer-version \
                        --layer-name $LAYER_NAME \
                        --zip-file fileb://layer_package.zip \
                        --compatible-runtimes python3.13 \
                        --description "Dependencies for Lambda - $ENV_NAME" \
                        --query Version --output text > layer.env
                      echo "Published layer version:"
                      cat layer.env
              - step:
                  name: Deploy_Lambda_Function
                  identifier: Deploy_Lambda_Function
                  type: Run
                  spec:
                    connectorRef: eksconnector
                    image: python:3.13
                    shell: Sh
                    command: |
                      ENV_NAME="<+pipeline.variables.environment>"
                      if [ -z "$ENV_NAME" ] || [ "$ENV_NAME" = "null" ]; then
                        ENV_NAME="dev"
                      fi
                      FUNCTION_NAME="hey-world-demo-$ENV_NAME"
                      ZIP_FILE="lambda_package.zip"
                      LAYER_VERSION=$(cat layer.env)
                      ROLE_ARN=""
                      if [ "$ENV_NAME" = "dev" ]; then
                        ROLE_ARN="arn:aws:iam::529088259986:role/lambda_exec_role_dev"
                      elif [ "$ENV_NAME" = "stage" ]; then
                        ROLE_ARN="arn:aws:iam::529088259986:role/lambda_exec_role_stage"
                      elif [ "$ENV_NAME" = "prod" ]; then
                        ROLE_ARN="arn:aws:iam::529088259986:role/lambda_exec_role_prod"
                      fi

                      if ! aws lambda get-function --function-name $FUNCTION_NAME > /dev/null 2>&1; then
                        aws lambda create-function \
                          --function-name $FUNCTION_NAME \
                          --runtime python3.13 \
                          --role $ROLE_ARN \
                          --handler devtest.lambda_function.lambda_handler \
                          --zip-file fileb://$ZIP_FILE \
                          --layers arn:aws:lambda:us-east-1:529088259986:layer:hey-world-deps-$ENV_NAME:$LAYER_VERSION \
                          --timeout 30 \
                          --memory-size 256
                      else
                        aws lambda update-function-code \
                          --function-name $FUNCTION_NAME \
                          --zip-file fileb://$ZIP_FILE
                        aws lambda update-function-configuration \
                          --function-name $FUNCTION_NAME \
                          --layers arn:aws:lambda:us-east-1:529088259986:layer:hey-world-deps-$ENV_NAME:$LAYER_VERSION \
                          --timeout 30 \
                          --memory-size 256
                      fi
              - step:
                  name: Setup_CloudWatch_Schedule
                  identifier: Setup_CloudWatch_Schedule
                  type: Run
                  spec:
                    connectorRef: eksconnector
                    image: python:3.13
                    shell: Sh
                    command: |
                      ENV_NAME="<+pipeline.variables.environment>"
                      if [ -z "$ENV_NAME" ] || [ "$ENV_NAME" = "null" ]; then
                        ENV_NAME="dev"
                      fi
                      FUNCTION_NAME="hey-world-demo-$ENV_NAME"
                      RULE_NAME="hello-world-schedule-$ENV_NAME"
                      SCHEDULE="rate(5 minutes)"
                      STATEMENT_ID="cw-invoke-$ENV_NAME"

                      aws events put-rule \
                        --name $RULE_NAME \
                        --schedule-expression "$SCHEDULE" \
                        --state ENABLED \
                        --description "Trigger Lambda every 5 minutes - $ENV_NAME"

                      FUNCTION_ARN=$(aws lambda get-function \
                        --function-name $FUNCTION_NAME \
                        --query 'Configuration.FunctionArn' \
                        --output text)

                      aws events put-targets \
                        --rule $RULE_NAME \
                        --targets "Id"="1","Arn"="$FUNCTION_ARN"

                      SOURCE_ARN=$(aws events describe-rule \
                        --name $RULE_NAME \
                        --query 'Arn' \
                        --output text)

                      if ! aws lambda get-policy --function-name $FUNCTION_NAME 2>/dev/null | grep -q $STATEMENT_ID; then
                        aws lambda add-permission \
                          --function-name $FUNCTION_NAME \
                          --statement-id $STATEMENT_ID \
                          --action 'lambda:InvokeFunction' \
                          --principal events.amazonaws.com \
                          --source-arn $SOURCE_ARN
                      fi
              - step:
                  name: Test_Lambda_Function
                  identifier: Test_Lambda_Function
                  type: Run
                  spec:
                    connectorRef: eksconnector
                    image: python:3.13
                    shell: Sh
                    command: |
                      ENV_NAME="<+pipeline.variables.environment>"
                      if [ -z "$ENV_NAME" ] || [ "$ENV_NAME" = "null" ]; then
                        ENV_NAME="dev"
                      fi
                      FUNCTION_NAME="hey-world-demo-$ENV_NAME"
                      RESPONSE=$(aws lambda invoke \
                        --function-name $FUNCTION_NAME \
                        --payload '{}' \
                        --cli-binary-format raw-in-base64-out \
                        lambda-response.json)
                      echo "Invoke response:"
                      echo "$RESPONSE"
                      cat lambda-response.json
                      if echo "$RESPONSE" | grep -q '"StatusCode": 200'; then
                        echo "Lambda test successful!"
                      else
                        echo "Lambda test failed!"
                        exit 1
                      fi
